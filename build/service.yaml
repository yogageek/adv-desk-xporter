apiVersion: v1
kind: Service
metadata:
  name: ifps-xporter-svc
spec:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    run: ifps-xporter
  sessionAffinity: None
  type: ClusterIP
status:
  loadBalancer: {}
